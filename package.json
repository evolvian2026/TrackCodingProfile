{
  "name": "track-coding-profile",
  "private": true,
  "version": "1.0.0",
  "description": "Competitive Programming Student Performance Tracker",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend",
    "typecheck": "npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend",
    "db:migrate": "npm run db:migrate --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend",
    "e2e": "npm run e2e:api && npm run e2e:ui && npm run e2e:session",
    "e2e:api": "tsx e2e/api.e2e.ts",
    "e2e:ui": "node e2e/ui.e2e.mjs",
    "e2e:session": "node e2e/session-race.e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.49.1",
    "tsx": "^4.19.2"
  }
}
